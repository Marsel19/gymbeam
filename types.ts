export interface ProductItem {
    positionId: string;
    x: string;
    y: string;
    z: string;
    productId: string;
    quantity: number;
}

export interface CoordinatesItem {
    x: string;
    y: string;
    z: string;
}

export interface DistanceItem {
    id: string;
    distance: number;
}
